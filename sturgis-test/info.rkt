#lang info

(define collection "tests")

(define build-deps '("base"
                     "sturgis-lib"))

(define update-implies '("sturgis-lib"))

(define pkg-desc "Tests for Sturgis")

(define pkg-authors '("jesse@serverracket.com"))

(define version "0.2")
