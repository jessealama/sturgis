#lang info

(define collection 'multi)

(define version "0.1")

(define deps
  '("sturgis-lib"
    "sturgis-doc"))

(define implies
  '("sturgis-lib"
    "sturgis-doc"))

(define pkg-authors '("jesse@serverracket.com"))
